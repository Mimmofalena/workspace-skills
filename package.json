{
  "name": "workspace-skills",
  "version": "0.2.0",
  "description": "One-shot AI skill installer for opencode and GitHub Copilot",
  "type": "module",
  "bin": {
    "workspace-skills": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
